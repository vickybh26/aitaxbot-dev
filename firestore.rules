rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is controlled server-side via Firebase Admin SDK.
    // Direct client access is not used.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
